name: color_match
description: 颜色匹配游戏
version: 1.0.0

environment:
  sdk: ">=2.19.0 <3.0.0"  # 对应 Flutter 3.16.9
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  vibration: ^3.1.0       # 确认兼容性：https://pub.dev/packages/vibration/versions
  audioplayers: ^5.1.0    # 确认兼容性：https://pub.dev/packages/audioplayers/versions

flutter:
  assets:
    - assets/sounds/
  uses-material-design: true
